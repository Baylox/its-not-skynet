# Meta — setup-securise

## Source
- Auteur : Baylox
- Statut : **stable**

## Contexte d'usage
Combine `pre_tool_use_block_secrets`, `no_coauthor` et `notification_desktop` en un setup cohérent.
Documente l'ordre d'exécution des hooks et fournit un `settings.json` prêt à copier.

## Installation

Copier le bloc `settings.json` depuis `setup-securise.md` dans votre config cible.

## Environnement testé
- Outil : Claude Code
